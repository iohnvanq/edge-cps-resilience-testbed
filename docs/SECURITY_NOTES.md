# Security Notes (Scope)

This repository is a research prototype.

- Do NOT commit secrets (tokens, keys, credentials).
- If using an encrypted overlay (e.g., VPN mesh), prefer “no public endpoints” wording.
- Service-level identity/authorisation is out of scope unless explicitly implemented.
- Firewall/ACL rules should be documented in a sanitised way (no real IPs/keys).
